export const API_CONFIG = {

  baseUrl: 'http://localhost:5294',

  endpoints: {
    referenceData:
      '/api/flights/reference-data',
    searchFlights:
      '/api/flights/search',
    persistedFlights:
      '/api/flights/persisted',
    bookings:
      '/api/flights/bookings'
  }
};